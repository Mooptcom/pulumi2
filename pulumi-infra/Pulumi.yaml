name: meteor-pulumi
description: a minimal meteor to cloudrun with esc
runtime:
  name: nodejs
  options:
    typescript: false
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: javascript
